dist: trusty
sudo: required
#language: cpp
compiler: gcc
script:
  - g++ main.cpp
  -
deploy:
  provider: releases
  api_key: ${GITHUB_TOKEN}
  file:
    - a.out
  skip_cleanup: true
  on:
    tags: true